# Rust build output
/target
# render PoC build output + result image
/render-poc/target
frame.png
*.png

# Node
/node_modules

# napi-rs build artifacts (regenerated by `napi build`)
*.node
index.js
index.d.ts

# Bundled libmpv binary lives here locally — do NOT commit it
/vendor/

# Stray libmpv binaries if placed at the folder root
libmpv-2.dll
mpv-2.dll
*.dll.a
*.pdb
