# Rust build output
/target

# Node
/node_modules

# napi-rs build artifacts (regenerated by `napi build`)
*.node
index.js
index.d.ts

# Bundled libmpv + ANGLE DLLs live here locally — do NOT commit
/vendor/
libmpv-2.dll
libEGL.dll
libGLESv2.dll
